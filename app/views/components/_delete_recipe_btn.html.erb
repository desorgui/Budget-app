<% if can? :destroy, recipe %>
  <%= button_to recipe_path(recipe), method: :delete, class: "c-icon-btn" do %>
    <svg class="c-icon--sm">
      <use xlink:href=<%= image_url("icons/sprite.svg#trash")%>></use>
    </svg>
  <% end %>
<% end %>