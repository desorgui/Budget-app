<%= form_with(model: @category_transaction, url: "/category/2/category_transaction/create", class: "c-new-recipe-form c-card") do |form| %>
  <% if @category_transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@category_transaction.errors.count, "error") %> prohibited this category_transaction from being saved:</h2>

      <ul>
        <% @category_transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :photo, style: "display: block" %>
    <%= form.text_field :photo %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :payment, style: "display: block" %>
    <%= form.text_field :payment %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
